<!DOCTYPE html>
<html lang="fr">
<head>
    <%- include("../partials/head") %> 
	<title>Mot de passe oublié</title>
	<link rel="stylesheet" type="text/css" href="/styles/forms.css" />
</head>
<body>
	<header>
        <a href="/" class="lefted image">
            <img src="/images/icon.png" alt="icone"/>
        </a>
		<nav>
            <ul>
                <li><a class="blue" href="/login">Connexion</a></li>
                <li><a class="blue" href="/register">Inscription</a></li>
            </ul>
        </nav>
	</header>
	<% if (typeof noUser !== "undefined" && noUser === true) { %> 
		<h2 class="error">Aucun utilisateur n'est associé à cet email</h2>
	<% } %>
	<% if (typeof invalidToken !== "undefined" && invalidToken === true) { %> 
		<h2 class="error">URL invalide</h2>
	<% } %>
	<form action="/forgotPW" method="POST">
		<h1>Mot de passe oublié</h1>
		<div>
            <label for="email">Email:</label>
            <input type="email" name="email" id="email" required autofocus />
		</div>
		<div>
            <input type="submit" value="Valider" class="blue" />
		</div>
		<p><a href="/login">Se connecter</a></p>
	</form>
</body>
</html>